{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug C++ (LLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/SDL_RayTracer",
            "args": [],
            "cwd": "${workspaceFolder}",
        }
    ]
}